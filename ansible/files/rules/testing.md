# Testing rules

- Run the full test suite before suggesting a commit.
- Add or update tests alongside any behavior change.
- Prefer fast, deterministic unit tests; isolate external services.
- A failing test must be reported with its output, never hidden.
